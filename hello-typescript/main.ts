// JS

var minhaVar = 'minha variavel';

function minhaFuncao (x, y)
{
    return x + y;
}

var numeros = [1, 2, 3];
numeros.map (function (valor) 
{
    return valor * 2;
})

// ES 6 ou ES 2015

let numero = 2;
const PI = 3.14;

numeros.map (valor => valor * 2);

class Matematica 
{
    soma (x, y)
    {
        return x + y;
    }
}

var str: string = 'string qualquer';
var num: number = 5;
var bool: boolean = false;
var qualquer: any = 'qualquer coisa';
qualquer = 10;
qualquer = true;